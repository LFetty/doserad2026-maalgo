# Serving-only dependencies for the proton CT/MRI challenge containers.
#
# torch, Triton, CUDA, cuDNN, and NumPy are supplied by the pinned PyTorch
# runtime base image. Keep these versions aligned with the previously validated
# full container while avoiding the training/evaluation dependency graph from
# pyproject.toml.
SimpleITK==2.5.3
scipy==1.17.1
blosc2==4.1.2
pydantic==2.12.5
pydantic-settings==2.13.1
fastapi==0.140.0
uvicorn[standard]==0.51.0
